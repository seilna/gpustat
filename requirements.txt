# All required dependencies must be specified in setup.py, not here.
# This list additional packages that will be installed BEFORE setup.py,
# in the python environment (e.g. Travis CI; see .travis.yml).

# Latest version (unreleased). expecting a release soon
git+https://github.com/jquast/blessed

# vim: set ft=conf:
